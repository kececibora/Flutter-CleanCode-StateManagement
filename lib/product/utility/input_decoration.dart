import 'package:flutter/material.dart';

class ProjectIntputs extends InputDecoration {
  const ProjectIntputs(String title)
      : super(
            border: const OutlineInputBorder(),
            labelText: title,
            focusColor: Colors.red);
}
